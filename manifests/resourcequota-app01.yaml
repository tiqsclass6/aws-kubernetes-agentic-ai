apiVersion: v1
kind: ResourceQuota
metadata:
  name: app01-quota
  namespace: app01

spec:
  hard:
    pods: "10"

    requests.cpu: "4"
    requests.memory: 8Gi

    limits.cpu: "8"
    limits.memory: 16Gi

    services: "5"
    secrets: "10"
    configmaps: "10"

    persistentvolumeclaims: "4"